const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const names = {supported:'Label matches',mismatch:'Different label',missing:'Image needed',unreadable:'Label unreadable',uncertain:'Needs a closer look'};
const roleNames = {existing:'Existing device',replacement:'Expected replacement',asset:'Target asset'};

/** Render validated, immutable review data. No network assets or executable scripts. */
export function renderReport(view) {
  const requirements = view.requirements ?? [];
  const unresolved = view.unresolvedCount;
  const resolved = requirements.filter(r => r.changed && r.status === 'supported' && r.previousStatus && r.previousStatus !== 'supported').length;
  const count = requirements.filter(r => r.status === 'supported').length;
  const headline = unresolved ? `${unresolved} ${unresolved === 1 ? 'item needs' : 'items need'} more evidence.` : 'The requested labels match.';
  const subtitle = unresolved ? 'Check the highlighted details and request what is missing.' : 'Review the observations before recording this check in chat.';
  const evidence = new Map((view.evidence ?? []).map(e => [e.evidenceId,e]));
  const priority = r => r.status !== 'supported' ? 0 : r.changed && r.previousStatus && r.previousStatus !== 'supported' ? 1 : 2;
  const cards = [...requirements].sort((a,b) => priority(a)-priority(b)).map((r,index) => {
    const o = r.observation;
    const e = o && evidence.get(o.evidenceId);
    const validImage = e && /^data:image\/(png|jpeg);base64,[A-Za-z0-9+/=]+$/.test(e.dataUrl ?? '');
    const region = o?.region;
    const validRegion = Array.isArray(region) && region.length === 4 && region.every(Number.isFinite) && region[0]>=0 && region[1]>=0 && region[2]>0 && region[3]>0 && region[0]+region[2]<=1.000001 && region[1]+region[3]<=1.000001;
    const rectangle = validRegion ? `<span class="region" style="left:${region[0]*100}%;top:${region[1]*100}%;width:${region[2]*100}%;height:${region[3]*100}%" aria-hidden="true"></span>` : '';
    return `<article class="requirement ${esc(r.status)}">
      <div class="card-top"><span class="step">${String(index+1).padStart(2,'0')}</span><span class="state ${esc(r.status)}">${esc(names[r.status] ?? r.status)}</span>${r.changed && r.previousStatus ? `<span class="change">${r.status==='supported' && r.previousStatus!=='supported'?'Resolved this round':'Updated this round'}</span>`:''}</div>
      <h2>${esc(r.label)}</h2><p class="role">${esc(roleNames[r.role] ?? r.role)}</p>
      <div class="compare"><div><span class="eyebrow">Requested</span><strong>${esc(r.expectedValue)}</strong></div><div><span class="eyebrow">Observed</span><strong>${esc(o?.observedValue || (r.status === 'missing'?'No image selected':'Not readable'))}</strong></div></div>
      ${validImage ? `<figure><div class="photo"><img src="${e.dataUrl}" alt="${esc(`Submitted image: ${e.label || e.originalName}`)}">${rectangle}</div><figcaption>${esc(e.label || e.originalName)} <span>${esc(e.evidenceId)}</span></figcaption></figure>` : `<div class="empty-image"><svg width="28" height="28" viewBox="0 0 24 24" fill="none" aria-hidden="true"><rect x="3" y="4" width="18" height="16" rx="3" stroke="currentColor" stroke-width="1.5"/><path d="m4 16 5-5 5 5 3-3 3 3" stroke="currentColor" stroke-width="1.5"/><circle cx="16" cy="9" r="1.5" fill="currentColor"/></svg><span>${e?'Image unavailable in this snapshot':'A clear image is still needed'}</span></div>`}
      ${o?.note ? `<p class="note">${esc(o.note)}</p>` : ''}
      <details><summary>Work-order reference</summary><p class="source">${esc(r.sourceQuote)}</p>${e?`<p class="hash">Image SHA-256<br>${esc(e.sha256)}</p>`:''}</details>
    </article>`;
  }).join('');
  const history = (view.history ?? []).map(h => `<li><span>${esc(h.reviewId)}</span><span>${Number.isInteger(h.unresolvedCount)?h.unresolvedCount:'?'} outstanding${h.recorded?' · recorded':''}</span></li>`).join('');
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data:; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'"><meta name="color-scheme" content="light"><title>${esc(view.job.title)} — Remote Hands Reviewer</title>
<style>
*{box-sizing:border-box}html{background:#f6f7f9;color:#18212f;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}body{margin:0;font-size:15px;line-height:1.5}a{color:#254fc4}header{background:#fff;border-bottom:1px solid #e5e8ee;padding:19px 5vw;display:flex;justify-content:space-between;align-items:center;gap:20px}.brand{display:flex;align-items:center;gap:11px;font-weight:650;letter-spacing:-.3px}.brand svg{color:#3656bd}.header-meta{color:#687284;font-size:12px;overflow-wrap:anywhere}.page{max-width:1400px;margin:auto;padding:38px 5vw 50px}.kicker{text-transform:uppercase;letter-spacing:.1em;font-size:11px;font-weight:700;color:#697389}.hero{padding-bottom:27px;border-bottom:1px solid #dfe4ec;margin-bottom:28px}.hero h1{overflow-wrap:anywhere;font-size:clamp(30px,4vw,48px);font-weight:600;letter-spacing:-1.7px;line-height:1.13;margin:12px 0 10px;max-width:900px}.hero p{color:#627086;margin:0}.metrics{display:flex;gap:20px;flex-wrap:wrap;margin-top:22px;font-size:13px}.metrics span{display:flex;gap:7px;align-items:baseline}.metrics b{color:#263e84}.metrics .attention b{color:#995311}.layout{display:grid;grid-template-columns:minmax(0,1fr) 300px;gap:25px;align-items:start}.cards{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px}.requirement{min-width:0;background:#fff;border:1px solid #dfe4ed;border-radius:12px;padding:22px}.card-top{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.step{font-family:ui-monospace,Menlo,monospace;color:#8691a3;font-size:11px;margin-right:auto}.state{display:inline-block;border-radius:5px;padding:3px 7px;font-size:11px;font-weight:600;background:#edf0f5;color:#586273}.state.supported{background:#edf5ee;color:#356444}.state.mismatch{background:#fff0de;color:#874913}.state.unreadable,.state.uncertain,.state.missing{background:#f1f2f6;color:#5c6680}.change{font-size:10px;color:#386547}.requirement h2{overflow-wrap:anywhere;font-size:18px;font-weight:600;letter-spacing:-.45px;margin:19px 0 2px}.role{font-size:12px;color:#6a7588;margin:0 0 18px}.compare{display:grid;grid-template-columns:1fr 1fr;gap:12px;border-top:1px solid #edf0f4;padding-top:15px;margin-bottom:19px}.eyebrow{display:block;font-size:10px;color:#748096;text-transform:uppercase;letter-spacing:.07em;margin-bottom:5px}.compare strong{display:block;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:16px;font-weight:550;overflow-wrap:anywhere;line-height:1.3}.mismatch .compare>div:last-child strong{color:#975318}.supported .compare>div:last-child strong{color:#386347}figure{margin:0}.photo{position:relative;background:#edf0f4;border-radius:7px;overflow:hidden;line-height:0}.photo img{display:block;width:100%;height:auto}.region{position:absolute;border:2px solid #d99425;background:#ffc65a10;box-shadow:0 0 0 1px #0002;pointer-events:none}.supported .region{border-color:#548463;background:#6dc58b0b}figcaption{font-size:10px;color:#738097;display:flex;justify-content:space-between;gap:10px;margin:7px 0 0;overflow-wrap:anywhere}.empty-image{min-height:170px;background:#f5f6f9;border:1px dashed #d5dbe5;border-radius:7px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;font-size:12px;color:#7b8597}.note{font-size:13px;color:#505e73;line-height:1.5;margin:16px 0 0;overflow-wrap:anywhere}details{border-top:1px solid #edf0f4;margin-top:18px;padding-top:12px}summary{font-size:11px;color:#617087;cursor:pointer;padding:3px 0}summary:focus-visible,a:focus-visible{outline:2px solid #4263c7;outline-offset:5px}.source{font-size:12px;color:#4c5870;white-space:pre-wrap;overflow-wrap:anywhere}.hash{font-family:ui-monospace,Menlo,monospace;font-size:9px;color:#7b8598;overflow-wrap:anywhere}aside{position:sticky;top:22px}.panel{background:#fff;border:1px solid #dfe4ed;border-radius:12px;padding:23px;margin-bottom:16px}.panel h2{font-size:15px;font-weight:600;letter-spacing:-.2px;margin:0 0 13px}.panel p{font-size:12px;color:#627087;margin:8px 0}.draft{font:inherit;font-size:13px;line-height:1.6;white-space:pre-wrap;overflow-wrap:anywhere;padding:14px 0 0;margin:0;border-top:1px solid #e9edf2;color:#334057}.draft-label{font-size:10px!important;letter-spacing:.08em;text-transform:uppercase;color:#728096!important}.record-note{background:#edf3ef;color:#355c44!important;padding:12px;border-radius:6px}.history{list-style:none;padding:0;margin:0;font-size:11px}.history li{display:flex;justify-content:space-between;gap:10px;border-top:1px solid #edf0f4;padding:10px 0;color:#6b778e}.history li:first-child{border:0}.history li span:first-child{font-family:ui-monospace,Menlo,monospace}.limits{font-size:11px!important;line-height:1.6!important}.work-order{white-space:pre-wrap;font:inherit;font-size:12px;line-height:1.6;color:#57657a;overflow-wrap:anywhere}.snapshot{margin-top:30px;border-top:1px solid #dfe4ec;padding-top:16px;font-size:10px;color:#768297;display:flex;justify-content:space-between;gap:15px;flex-wrap:wrap;overflow-wrap:anywhere}@media(min-width:1300px){.page{padding-left:45px;padding-right:45px}}@media(max-width:1000px){.layout{grid-template-columns:minmax(0,1fr) 270px}.cards{grid-template-columns:1fr}}@media(max-width:720px){header{padding:16px 20px;align-items:start;flex-direction:column;gap:4px}.page{padding:25px 20px}.layout{grid-template-columns:1fr}.cards{grid-template-columns:1fr}aside{position:static}.hero h1{letter-spacing:-1px}.requirement{padding:20px}.metrics{gap:12px}.snapshot{font-size:9px}}@media print{html{background:#fff}.page{padding:18px}.layout{display:block}.cards{grid-template-columns:1fr 1fr}aside{position:static;display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:20px}.requirement,.panel{break-inside:avoid}.photo img{height:auto}header{padding:12px 18px}}
</style></head><body>
<header><div class="brand"><svg width="26" height="26" viewBox="0 0 26 26" fill="none" aria-hidden="true"><rect x="2" y="2" width="13" height="13" rx="3" stroke="currentColor" stroke-width="1.7"/><rect x="11" y="11" width="13" height="13" rx="3" fill="currentColor"/><path d="m14.5 17.3 2 2 4-4" stroke="white" stroke-width="1.5"/></svg>Remote Hands Reviewer</div><div class="header-meta">${esc(view.job.jobId)} · ${esc(view.reviewId)}</div></header>
<main class="page"><section class="hero"><div class="kicker">${esc(view.job.title)}</div><h1>${headline}</h1><p>${subtitle}</p><div class="metrics"><span><b>${count}/${requirements.length}</b> labels supported</span><span class="attention"><b>${unresolved}</b> outstanding</span>${resolved?`<span><b>${resolved}</b> resolved this round</span>`:''}</div></section>
<div class="layout"><div class="cards">${cards}</div><aside>
<section class="panel"><h2>${unresolved?'Ask for the missing evidence':'Ready for owner review'}</h2>${unresolved?`<p class="draft-label">Draft · not sent</p><pre class="draft">${esc(view.followUpDraft)}</pre>`:`<p class="record-note">All requested identifiers have supporting observations.</p><p>Record this review in chat after checking the images. This does not accept the physical work or close the ticket.</p>`}</section>
<section class="panel"><h2>Review history</h2>${history?`<ul class="history">${history}</ul>`:'<p>First review for this job.</p>'}<p>Current snapshot: ${esc(view.reviewId)}</p></section>
<section class="panel"><details style="margin:0;padding:0;border:0"><summary>Original work order</summary><pre class="work-order">${esc(view.job.workOrder)}</pre></details><p class="limits">Checks visible identifiers in the supplied images. It does not verify installation, hidden connections or image freshness.</p></section>
</aside></div><footer class="snapshot"><span>Status when generated: ${esc(view.status==='ready_to_record'?'Ready for owner review':'Evidence outstanding')} · Version ${esc(view.version)}</span><span>${esc(view.generatedAt)} · Ask in chat for current status.</span></footer></main></body></html>`;
}
